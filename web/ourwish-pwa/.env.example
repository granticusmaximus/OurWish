# Empty keeps API calls same-origin in production and uses the Vite proxy in local dev.
VITE_API_BASE_URL=

# Optional override for `npm run dev` when the backend is not on localhost:8420.
VITE_DEV_API_PROXY_TARGET=http://localhost:8420
