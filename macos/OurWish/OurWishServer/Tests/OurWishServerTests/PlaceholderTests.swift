// Placeholder so `Tests/OurWishServerTests` isn't empty. Route-level correctness is
// verified via `swift run RunServer` + `curl`, the same command-line-first approach
// used throughout this project (this Mac only has Xcode Command Line Tools, not the
// full Xcode.app needed for XCTest/swift-testing to run via `swift test`).
